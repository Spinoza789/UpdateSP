# Review task: approved contracts and selectors

Read-only review in `/home/amoney/UpdateSP`. Do not edit files.

Review these files against `implement_contracts_selectors_TASK.md`:

- `artifacts/peps-anonymous/src/pages/organiser-v2/approved-workspace.test.ts`
- `artifacts/peps-anonymous/src/pages/organiser-v2/approved-selectors.test.ts`
- `artifacts/peps-anonymous/src/pages/organiser-v2/approved-selectors.ts`

Stage 1 is spec compliance only. Check every requirement, including edge cases,
readiness vocabulary, lane identifiers, deterministic/empty trend behaviour,
and whether the package test command was added. Run the focused selector test.

Return exactly one of:

- `APPROVED` with evidence; or
- `CHANGES REQUIRED` with a concise, prioritized list of missing/extra/wrong items.
