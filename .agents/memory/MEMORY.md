- [Vite 504 dep optimisation on .replit.dev](vite-504-dep-optimization.md) — clearing .vite cache causes 504s via edge proxy; fix is optimizeDeps.entries listing all pages.
- [Multi-chain crypto payment rails](multi-chain-crypto-rails.md) — chain choice stored as paymentCryptoNetwork; CHAIN_WALLET_CONFIGS drives all resolution; getAdminCryptoOptions() exported for auto-verifier; pickedOption={currency,network} replaces pickedCurrency in PaymentPanel.
- [Organiser V2 placeholder data traps](organiser-v2-placeholder-data.md) — mapApiGroupBuy fills fake fallbacks (maxMembers=1, counts 0); wire V2 tabs from raw API values via props, never SampleGB counts.
- [Organiser V2 drawer portal CSS scoping](ov2-drawer-portal-scoping.md) — mobile drawer is a portalled Sheet; `.organiser-v2 .ov2-sidebar-drawer` selectors never match it, use compound `.organiser-v2.ov2-sidebar-drawer`.
- [Discord synthetic username display](discord-synthetic-username-display.md) — own-account handle display must use getAccountHandle()/isDiscordOnlyAccount(), never raw telegramUsername; localStorage-key scoping stays raw.
- [Lab test extract blob preference](lab-test-extract-blob-preference.md) — CoA re-extract (single+batch) must try stored pdfBlob before external url; Cloudflare-blocked sources always fail otherwise even with a local copy.
- [GB entry-fee join gate](gb-entry-fee-join-gate.md) — membership only inserted after fee confirmed (402 ENTRY_FEE_REQUIRED gate in join handler); any new join path must route through same gate.
- [GB entry-fee confirm atomicity](gb-entry-fee-confirm-atomicity.md) — confirm status-update + membership-grant must be one transaction w/ idempotent retry, or payment shows confirmed forever with no membership.
- [Duplicated join-modal entry-fee gap](duplicated-join-modal-entry-fee-gap.md) — Groups.tsx & CustomerPortal.tsx each have their own copy of "Join a Group Buy"; a handler fix ported to one silently missed the other.
- [PageLayout Portal-nav vs DashboardShell](pagelayout-portal-nav-vs-dashboardshell.md) — /order & /review show Hub/Orders/GB sidebar labels even for guests; only search/bell/credits header row signals real DashboardShell chrome.
- [Blood test parser ordering rules](blood-test-parser-ordering.md) — NHDL before HDL, VLDL before LDL, ACTB12 before VITB12, CKMB before CK; HDL notWith "non", LDL notWith "vldl", FT3 notWith "total".
- [Blood test US unit normalization](blood-test-us-units.md) — normalizeUnits() in parsePDF.ts converts ng/dL, pg/mL, mg/dL, g/dL, NGSP% to SI/UK catalog units on import.
- [Production startup migrations](prod-startup-migrations.md) — How production schema changes work and what goes wrong when columns are missing.
- [Uzorak bulk import fix](uzorak-import.md) — Uzorak 302-redirects /verify/X.pdf back to SPA hash; bypass via Supabase RPC, not direct download.
- [protobufjs-blocked](protobufjs-blocked.md) — Replit firewall blocks protobufjs@7.5.4; replaced @google/genai with a local REST shim at lib/google-genai-shim.
- [Gemini local module](gemini-shim-api-version.md) — pnpm workspace alias for @google/genai is broken; use local src/lib/google-genai.ts (OpenAI-compatible /chat/completions).
- [Build vs typecheck separation](build-typecheck-separation.md) — build script decoupled from typecheck; recurring zod v4, Express 5 params, and api-zod codegen issues documented.
- [GitHub push from Replit](github-push-replit.md) — Replit askpass blocks git push; bypass with GIT_ASKPASS="" + token in URL + lfs.locksverify=false. Rebase --continue must go via Project Task.
- [Lab test preview embeddability](lab-test-preview-embeddability.md) — Janoshik: Cloudflare+SAMEORIGIN → link only. Uzorak: no X-Frame-Options → iframe directly when no snapshot/pdf.
- [Janoshik import via browser helper](janoshik-cloudflare-import.md) — Janoshik Cloudflare-blocks server fetches (403); import only via admin bookmarklet → receiver → bookmarklet-import; single + bulk (2-tab postMessage worker, same-host only); preview/proxy prefer stored blob.
- [Testing pool mobile overflow](testing-pool-mobile-overflow.md) — TestingPool/GbTestingPool wrap in overflow-x-hidden (clips, not scrolls); long compound labels get cut off → need min-w-0+break-words; use lg:grid-cols-2 not md:.
- [Lab certificate compression](lab-cert-compression.md) — all pdfBlob writes funnel through prepareCertificateForStorage (→WebP q85); compress stored bytes only, extract on originals; idempotent via ≤90% keep rule.
- [Lab-test list endpoints exclude pdfBlob](lab-tests-list-no-blob.md) — list/bulk lab_tests queries must project columns explicitly and omit pdfBlob; bare select() bloats response to ~105MB and silently breaks /tests page. Images load via /proxy.
- [Wholesale share status races](wholesale-share-status-races.md) — lock/cancel/submit/delivery writes each need conditional status-gated updates (→409); only the chosen recipient sets the address.
- [Wholesale share max-people cap](wholesale-share-max-members.md) — max_members nullable=no limit; new shares uncapped, existing keep 10 (not migrated); PUT cap min 2 (lock needs ≥2), ≤1000, ≥ current count.
- [Workflow topology](workflow-topology.md) — only "Start application" + mockup-sandbox are canonical; api-server/web workflows are duplicates that FAIL on port conflict if restarted alongside it. Restart only "Start application" to reload secrets.
- [Dev-domain 502 vs working preview](dev-domain-502-edge-routing.md) — public *.replit.dev can 502 after heavy restarts/DB ops while app is healthy; confirm via localhost+Host-header 200 and app_preview; it's an edge artifact, verify flows via screenshot not runTest.
- [Drizzle push rename prompt](drizzle-push-interactive-rename.md) — drizzle-kit push (even --force) prompts create-vs-rename when DB has orphan tables; hangs in non-TTY agent shell → add a single new table via direct SQL DDL matching the schema.
- [Group-buy visibility gate](gb-visibility-gate.md) — un-hiding a GB to public lists must be server-gated on approvalStatus==='approved' OR admin, else organisers self-publish unapproved GBs; hidden means list-excluded, still joinable by id+PIN.
- [GB admin fee fixed vs percent](gb-admin-fee-percentage.md) — order stores RESOLVED fee; percent applies to product subtotal, resolved only at create/edit/backfill; edits never add a fee to an order that never had one; direct-shipping zeroes it; EVERY grandTotal recompute site (not just the main edit endpoint) must include+persist it.
- [Role change side-effects](role-change-side-effects.md) — any path mutating account roles must mirror single-account handler side-effects (organiser audit log + notify + clear organiserApprovedAt on revoke); strictly validate action/role.
- [FS3 TXT package pricing](fs3-txt-package-pricing.md) — order TXT package line uses active wholesale vendor regional kit-count tiers (25/pkg); reshipper leg stays flat $70 on purpose (no reliable destination country).
- [Shared-wholesale admin grouping](shared-wholesale-admin-grouping.md) — admin order list merges same-sharedOrderId wholesale_shared orders into one lead-labelled parent row; client-side, page-window bound, wholesale/all views only.
- [My Orders page location](my-orders-page-location.md) — customer My Orders tabs = CustomerPortal.tsx (section "orders", /account?s=orders); /my-orders (AccountOrders.tsx) is a separate secondary page.
- [Crypto verify tolerance](crypto-verify-tolerance.md) — once crypto amounts are locked-rate converted (display==verify), all verifyTransaction paths must use ~1% default tolerance, never 15%; loose tolerance is a money-integrity risk and gets code-review rejected.
- [Crypto lock reset on edit](crypto-lock-reset-on-edit.md) — every total-changing order edit must clear paymentUsdAmount (unless paid-like: confirmed/test_confirmed); a 3% staleness check alone leaks small edits.
- [ERC-20 stablecoin currency authority](erc20-stablecoin-currency-authority.md) — customer USDT/USDC choice only on ETH ERC-20 rail; validate vs server options, persist + re-read at verify, never trust client currency.
- [Reshipper dispatch orders scope](reshipper-dispatch-orders-scope.md) — /reshipper/dispatch/orders runs adminOrdersHandler; reshipper scope must narrow by ownership (reshipperUsername OR dispatchedByReshipper==me), NOT country-leg, or it leaks all reshippers' orders.
- [Order status timeline stepper](order-timeline-stepper.md) — fulfilment stepper "current" must reflect real status only (never over-claim); Paid is a payment overlay; omit fields data lacks.
- [Dispatch factory router auth scoping](dispatch-router-auth-scoping.md) — root-mounted role-scoped dispatch routers must path-scope their auth middleware (router.use(cfg.prefix,...)) or admin's requireAdmin 503s sibling /reshipper|organiser/dispatch/* before they reach their own router.
- [api-server vitest + db mock](api-server-vitest-db-mock.md) — mock @workspace/db via schema-only export (no Pool); scoped routers use path-less auth, so mount only the router under test, not both.
- [Dispatch parcel gating](dispatch-parcel-gating.md) — GB dispatch reshipper list + Order Overview must not be gated on delivered parcels; union active-order reshippers, render overview for reshipper+all scopes with zero parcels.
- [Shared dispatch admin parity](shared-dispatch-admin-parity.md) — AdminDispatch.tsx is shared by admin/reshipper/organiser; any UI addition must be gated by cfg.role to keep admin byte-identical.
- [Order additions / top-ups](order-additions-topup.md) — additions persist additionOfOrderId marker; server forces free shipping + locked parent address on POST/PUT/shipping-address; require parent paymentStatus==='confirmed'; UI zeroes vendor shipping too.
- [Wholesale shared guidance lockstep](wholesale-shared-guidance-lockstep.md) — presentation-only next-step banner/wizard/tracker must mirror page readiness/money checks exactly, else "all set while fee owed" money-integrity UX bugs.
- [Wholesale onward roster gate](wholesale-onward-canconfirm-gate.md) — onward roster/Manage UI must AND share.onward.canConfirm with share.onward.enabled (backend sets canConfirm independent of enabled); config path uses canManage, not enabled.
- [Artifact typecheck stale lib decls](api-server-typecheck-stale-libs.md) — after editing lib/db (or lib/api-zod) schema, run `tsc --build`/`typecheck:libs` first or artifact typecheck phantom-errors "property does not exist" (runtime via tsx/src is fine).
- [Wholesale draft autosave lifecycle](wholesale-draft-autosave-lifecycle.md) — clear draft only on submit success, save null when empty, clear timer on unmount/nav, abort in-flight PUTs (latest-write-wins) or a cleared/discarded draft resurrects.
- [Onward shipping & body-limit gotcha](onward-shipping-body-limit.md) — wholesale onward charge is peer-to-peer (never in grandTotal); any base64-image route MUST join a high-limit express.json group or the global 64kb cap silently 413s it.
- [Wholesale shared fee separation](wholesale-p2p-fees.md) — organiser fees belong in applicable order totals; reshipper fees remain P2P; recipient exempt.
- [Username rename migration gaps](username-rename-migration-gaps.md) — non-FK username text columns need explicit migration in change-username route; use set-based self-heal (via a sibling already-migrated column) to fix stranded prod rows without direct writes.
- [Order receipt client vs server totals](order-receipt-client-vs-server-totals.md) — post-submit payment screen reads a client-written localStorage receipt; must prefer server response money fields over pre-submission draft math.
- [Wholesale shared shipping split integrity](wholesale-shared-shipping-split.md) — per-member vendorShipping is a cross-member split snapshotted at lock; never recompute from one order; block shared-order edits + reconcile grandTotal from member shipping_share snapshot.
- [Wholesale onward tracking (masked)](wholesale-onward-tracking.md) — mask server-side in buildShareResponse; UI gate on server canEditTracking not creator flag; conditional writes + per-number guard on async fetch.
- [Wholesale per-member onward address](wholesale-onward-member-address.md) — each member's onward forwarding address is private to recipient+owner only (gated in buildShareResponse); recipient can't set one; not vendor-priced; conditional status-gated write.
- [Wholesale lab-report batch-prefix matching](wholesale-lab-batch-prefix-matching.md) — CoAs match by batch prefix at exact DOSE level (never other strengths); vendor table stale vs live data (verify DB labels before adding); ambiguous blends unmatched.
- [Wholesale MAIN-parcel masked tracking](wholesale-main-tracking.md) — orders.trackingNumber is truth, share.mainTracking* is a cache keyed to it; non-recipients get masked+number-HIDDEN (privacy note, like onward); conditional cache-key writes + dirty-rerun + scan self-heals stale terminal keys; never fetch in the polled GET.
- [Admin order tracking write side-effects](admin-order-tracking-notify-side-effects.md) — PATCH tracking auto-sets Shipped + notifies customer with no old/new compare; bulk/group tracking writes MUST skip members already at the value or they spam duplicate notifications.
- [Wholesale order shipping server-authority](wholesale-order-shipping-authority.md) — customer wholesale POST/PUT must recompute vendorShipping from tier table (never trust client); fallback-to-client only for non-calculable regions is intentional.
- [GB testing-pool winner counting](gb-testing-winner-counting.md) — leading compound must tally ALL peptideNames per vote (not first-pick peptideName) via shared getLeadingVote, or multi-compound ballots name a different winner than the public page shows.
- [Wholesale publish vs settings persistence](wholesale-publish-settings-persistence.md) — maxPackages/organiserFlatFee live in main settings form; flat-fee re-resolution (settings+publish) must be txn+row-lock, isPublic-gated, reset paid flag, idempotent, recipient read from LOCKED row; never clear settingsDirty on publish.
- [Wholesale public groups](wholesale-public-groups.md) — public shared orders: first-publish Telegram announce detected under row lock; topic needs BOTH chat+topic envs; creator+recipient fee-exempt.
- [Sage discuss daily quota](sage-discuss-daily-quota.md) — /blood-tests/discuss is N/day (default 10) via atomic UPDATE on discuss_count+discuss_count_date; clamp limit ≥1, day-conditional rollback, exhaustion MUST return 429 error:"limit_reached".
- [Lab report batch-prefix matching](lab-report-batch-prefix-matching.md) — lab_tests.mgAmount is measured potency not label dose; always match reports via batch-code prefix, never exact mgAmount/name.
- [Canvas scroll phantom bug](canvas-scroll-phantom-bug.md) — "dashboard won't scroll" where scrollbar-drag works but trackpad/wheel doesn't = Replit board capturing wheel over the iframe tile; app scrolls fine, open in a real tab; don't fix in code.
- [Mobile drawer body-lock breaks desktop scroll](mobile-drawer-body-lock.md) — a global md:hidden drawer's body-overflow lock must be viewport-scoped or a stale mobile open freezes desktop scroll; zustand setOpen takes a boolean, setOpen(o=>!o) stores the function (permanently truthy).
- [Portal lab-tests vs blood-tests](portal-labtests-vs-bloodtests.md) — two DIFFERENT CustomerPortal sections: blood-tests=member bloodwork (KPI counts bloodTestCount), lab-tests=product CoA reports (useListLabTests); label nav by destination.
- [Customer home sole-entry-point widgets](customer-home-orphan-entrypoints.md) — home redesigns must keep Special Access (/qr-viewer,/leg-view — no other link), store credits, and organiser GB Metrics (/gborganiser) or those flows orphan.
- [DashboardHome desktop dual-pane scroll](dashboard-desktop-dual-scroll.md) — desktop main content must OWN its scroll (lg-gated block overflow-y-auto+overflow-x-hidden, root lg:overflow-hidden); never body-scroll coupling; mobile stays natural scroll. Also mobile header px-2→sm:px-2 fix.
- [Sage dashboard chat](sage-dashboard-chat.md) — "Meet Sage" hero reuses POST /blood-tests/discuss (never a parallel AI endpoint); the compounds-only no-session fallback must stay parity with the main path (quota+rollback, guardrail, audit logging, response shape).
- [Dashboard shell section pattern](dashboard-shell-section-pattern.md) — CustomerPortal sections migrate to shared DashboardShell (palette(dark) chrome + T-based content untouched); one [data-theme] flag drives both; 3-col StatCards need SHORT labels.
- [hexToRgba vs CSS-var colors](hexToRgba-vs-css-var-colors.md) — STATUS_META/PAYMENT_META colors are CSS vars (var(--t-blue)); hexToRgba on them → rgba(NaN) dropped; use color-mix for tints, hexToRgba only for hex accents.
- [Design subagent functional regressions](design-subagent-functional-regressions.md) — DESIGN redesigns of big pages silently gut forms/deep-links/server fetches; diff-audit vs git HEAD, splice originals back verbatim.
- [Hub nav breakpoint pairing](hub-nav-breakpoint-pairing.md) — HubBottomNav hideAt must match host layout's sidebar breakpoint (PageLayout=md, DashboardShell=lg) or tablet loses all nav; class + matchMedia in lockstep.
- [Site typography attempts](site-typography.md) — blanket font swap was tried and REJECTED ("looks horrible"); brainstorm + canvas mockups + approval before any site-wide restyle; fonts load in index.html AND index.css @import.
- [In-app notifications feed](in-app-notifications-feed.md) — bell feed reads telegram_message_logs; every notify path must logTgMessage even on skip (account lookup BEFORE token early-return) or unlinked users see nothing.
- [DashboardShell bottom bars](dashboard-shell-bottom-bars.md) — bottom bars in shell pages must be sticky (lg:bottom-0 + mobile pill clearance), never fixed with hardcoded sidebar offset; collapse state is internal.
- [Mockup sandbox verify + presentArtifact id](mockup-sandbox-verify.md) — preview-page url 200s even when the module 500s; curl /__mockup/src/.../<Comp>.tsx or screenshot EACH variant; presentArtifact needs the artifactId from the error's "Available artifacts" list (sandbox = "Component Preview Server").
- [Dashboard-align var(--t-*) pages](dashboard-align-css-var-pages.md) — a page already on var(--t-*) tokens needs NO palette()/color conversion to match dashboard; only hero (HERO_GRAD+STATUS_STYLE), radius 16→8, and SecIcon headers.
- [Dashboard theme toggle vs Tailwind dark:](dashboard-theme-toggle-vs-tailwind-dark.md) — --t-* flips via manual data-theme (default light, no OS); Tailwind dark: keys off OS (no config) → decoupled; use CSS light-dark() for semantic accents on --t-surface cards, not dark:.
- [Health Hub dashboard redesign](health-hub-dashboard-redesign.md) — Hub landing + compounds/health/glp1 sections use HERO_GRAD band + StatCard(palette(dark)) + SecIcon groups; import HERO_GRAD/SecIcon into CustomerPortal (vite won't hard-fail on missing symbol, crashes at render behind login).
- [Embedded app host chrome vs iframe fixed UI](embedded-fixed-position-iframe-chrome.md) — never overlay host buttons on a full-viewport iframe with its own fixed-position corners; give host UI a separate header bar instead.
- [Mockup sandbox recharts+React19 cache](mockup-sandbox-recharts-react19-cache.md) — first recharts mockup can throw "Invalid hook call" from stale .vite optimize-deps cache; rm -rf node_modules/.vite + restart fixes it, not a real incompatibility.
- [Mockup registry Git churn](mockup-registry-git-churn.md) — sandbox registry discovery must sort paths; unsorted glob output lets duplicate preview servers rewrite a tracked file and blocks Git pulls.
- [Sage AI model admin override](sage-ai-model-admin-override.md) — explicit model param skips fallback chain; a stale site_config sage_ai_model row can pin a proxy-account-unsupported model, breaking ALL Sage calls with 503 model_not_found.
- [Embedded app absolute asset paths](embedded-app-absolute-asset-paths.md) — third-party build iframed under a subpath can have runtime fetches hardcoded to site-root paths (base:'./' doesn't cover these); mirror the files at the expected absolute path.
- [Lab test title resolution sites](lab-test-title-resolution-sites.md) — 3 duplicate title builders (LabTests.tsx, PrototypeLabTests.tsx, LabTestsPopup.tsx) must stay in sync; popup had zero batch-code resolution before.
- [Sage/Claude proxy silently drops PDF document blocks](sage-proxy-pdf-document-blocks.md) — rasterize PDFs to an image (pdftoppm) before sending to Claude via the Sage proxy; native "document" content blocks are ignored, not errored.
- [Lab test field multilayer drop](lab-test-field-multilayer-drop.md) — "field missing" bugs can hide independently in prompt/mapping/payload/backend-insert; trace field name through all 4 layers + backfill existing rows.
- [Order preview draft isolation](order-preview-draft-isolation.md) — preview/read-only order-form modes must guard EVERY persisted-draft write or they wipe the user's real in-progress draft; hide member-only action prompts too.
- [Dev JWT auth-endpoint testing](dev-jwt-testing-auth-endpoints.md) — no dev-login route, but account_session routes are curl-testable: sign JWT with the dev fallback secret; DB lookups via executeSql (pg not requirable).
- [Dashboard logo asset reuse](dashboard-logo-asset-reuse.md) — when matching dashboard branding, reuse its exact icon rail, SaltPepsMark, and /brand wordmark asset; never rebuild the lockup with styled text.
- [Email template system](email-template-system.md) — DB-driven templates in email_templates table; sendTemplatedEmail() is the canonical send path; dynamic import pattern for all route hooks; requireOrganiser is Express middleware (not a guard), use it as router.post("/path", requireOrganiser, handler).
- [Shared-order admin override](shared-order-admin-override.md) — admin controls must reuse creator workflows after admin-secret validation, never bypass transactional order safeguards with direct writes.
- [Locked-order payment protection](locked-order-payment-protection.md) — every post-lock reconciliation must protect all non-unpaid orders from decreases or payment-lock resets.
- [Orderless reopened-member item guard](orderless-reopened-member-item-guard.md) — never interpolate a nullable order ID into `$n IS NULL`; use a constant SQL gate for orderless members.
- [Deployment foreign-key action drift](deployment-foreign-key-action-drift.md) — direct deploy SQL can override Drizzle FK actions; keep it aligned with the schema or migration previews remain stale.
- [Shared-order organiser wallet edit access](shared-order-organiser-wallet-edit-access.md) — a configured lead wallet is a member payment destination, not proof the organiser has paid or cannot add items while open.
- [Shared-order payment transaction privacy](shared-order-payment-transaction-privacy.md) — member transaction IDs and received amounts belong only in organiser-gated responses, never merely hidden in UI.
